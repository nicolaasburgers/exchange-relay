{
  "name": "@exchange-relay/azure-openai-relay",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "watch": "tsc -w",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.5.0",
    "@exchange-relay/common": "file:../../packages/common"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1",
    "azure-functions-core-tools": "^4.0.5611"
  }
}