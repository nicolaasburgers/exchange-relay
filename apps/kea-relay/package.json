{
  "name": "@kea-relay/kea-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Neutral Kea relay with AWS Bedrock as first provider adapter",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "clean": "rimraf dist",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "prestart": "npm run clean && npm run build",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.869.0",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.12",
    "rimraf": "^6.0.1",
    "tsx": "^4.20.6",
    "typescript": "^5.6.3"
  }
}
